import 'package:flutter/material.dart';

/// Stub scaffold — replaced in Task 6 with the full diff-centric UI.
class LeakRadarMainScaffold extends StatelessWidget {
  const LeakRadarMainScaffold({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Leak Radar — coming soon')),
    );
  }
}
