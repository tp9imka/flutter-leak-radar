name: radar_native_host
description: >-
  Host-side parser turning heapprofd .pftrace captures into radar_native
  NativeHeapProfile checkpoints.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

resolution: workspace

dependencies:
  radar_native: ^0.1.0
  radar_trace: ^0.2.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
